const path = require('path')

const FUNC_FILE_NAME = 'func.js'
const FUNC_OPTIONS_FILE_NAME = 'options.json'
const FUNC_ID_LENGHT = 16
const ROOT_PATH = path.join(__dirname, '..')

module.exports = {
  FUNC_FILE_NAME,
  FUNC_OPTIONS_FILE_NAME,
  FUNC_ID_LENGHT,
  ROOT_PATH,
}
